when:
  event:
    - pull_request
    - push
    - commit
  depends_on:
    - install-dependencies

steps:
  - name: security-scan
    image: node:21.6.2-alpine
    commands:
      - pnpm run security:scan
