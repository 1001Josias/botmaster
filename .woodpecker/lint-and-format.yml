image: node:21.6.2-alpine

when:
  event:
    - pull_request
    - push
    - commit
  depends_on:
    - install-dependencies

steps:
  - name: lint
    commands:
      - pnpm run lint:all

  - name: format-check
    commands:
      - pnpm run format:all
