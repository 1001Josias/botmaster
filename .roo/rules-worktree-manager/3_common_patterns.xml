<common_patterns>
  <pattern name="branch_naming">
    <description>Generate branch names using the format: type/name</description>
    <example>
      <input>type: feature, name: login</input>
      <output>feature/login</output>
    </example>
  </pattern>
  <pattern name="git_worktree_command">
    <description>Create a new worktree and branch in one step</description>
    <example>
      <input>branch: feature/login</input>
      <output>git worktree add -b feature/login ../feature/login</output>
    </example>
  </pattern>
  <pattern name="open_in_vscode">
    <description>Open the new worktree in a separate VS Code window</description>
    <example>
      <input>worktree path: ../feature/login</input>
      <output>code ../feature/login</output>
    </example>
  </pattern>
</common_patterns>
